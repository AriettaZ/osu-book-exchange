<!-- Author: Gail Chen
Created: 7/24
Edit: N/A
Description: When the admin is creating a contract, update the options of sellers
after a post is selected using ajax request. -->

<%= label_tag "Seller ID" %>
<% if @sellers.length > 1%>
  <%= collection_select :contract, :seller_id, @sellers, :id, :id, prompt: "--select--" %>
<% else %>
  <%= collection_select :contract, :seller_id, @sellers, :id, :id%>
<% end %>
